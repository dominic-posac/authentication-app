export interface MailDataInterface {
  user: string;
  to: string;
  subject: string;
  html: string;
}

export interface UserDataInterface {
  email: string;
  firstName: string;
  lastName: string;
}